# Major revision hardening contract

The reviewer marked reproducibility insufficient and objected to workstation-local paths. This artifact closes that class of issue by making paths relocatable, content identities explicit, validation executable, and the paper-facing claim surface receipt-driven.

A historical PASS is never upgraded to R1 VERIFIED by copying a number. Required gates must produce a new receipt.
