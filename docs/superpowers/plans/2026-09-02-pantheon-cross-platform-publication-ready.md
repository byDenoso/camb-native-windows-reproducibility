# Pantheon Cross-Platform Publication Closure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove the false Pantheon+ byte-identity blocker without weakening provenance, run the complete R01-R16 robustness battery, and promote publication readiness only from verified machine-readable receipts.

**Architecture:** Keep source identity fail-closed at the frozen Pantheon+ Git commit and exact table/covariance hashes. Treat generated Cholesky/NPY bytes as platform-local build artifacts and certify them by reconstruction residuals, direct-vs-factor quadratic-form equivalence, exact selected-array round trips, and deterministic same-platform rebuilds. Publication promotion remains receipt-driven; no gate is manually flipped.

**Tech Stack:** CPython 3.12.13, NumPy 2.5.1, SciPy 1.18.0, Pandas 3.0.5, pytest 9.0.3, GitHub Actions Windows 2025 runner.

**Spec:** `docs/superpowers/specs/2026-09-02-ascom-00323-hardening-design.md`

## Global Constraints

- Exact Python acceptance version: 3.12.13.
- CAMB upstream commit: `3ef0272d6f7ba1231128872e56e6d4c12af8267b`; CAMB version: 1.6.6.
- Pantheon+ commit: `c447f0fea703fcd0fff57de5000947b5ca81286b`.
- Pantheon+ source table SHA-256: `1cb0fc379ef066afdc2ffd1857681cc478024570d8a3eba284fb645775198cf8`.
- Pantheon+ source covariance SHA-256: `abf806d966485e64afdb359c87bffc0ecc00d05eff0a31ced66f247385df0fdc`.
- Scientific likelihood tolerance remains `1e-9`; CAMB probe tolerance remains `1e-12`.
- Never promote historical values as fresh evidence.
- `PUBLICATION_READY` may become true only from verified required receipts.

---

### Task 1: Pantheon compiled-science contract

**Files:**
- Create: `tests/test_pantheon_cross_platform_contract.py`
- Modify: `scripts/run_r1_scientific_closure.py`
- Modify: `scripts/run_r1_robustness.py`

**Interfaces:**
- Consumes: frozen Pantheon table/covariance source bytes and selected covariance matrices.
- Produces: `verify_pantheon_factorization(selected_cov, factor, residual_vectors) -> dict`, compilation receipt fields `scientific_equivalence`, `max_covariance_reconstruction_abs`, `max_quadratic_form_abs_delta`, and platform-local `compiled_manifest_sha256`.

- [ ] **Step 1: Write the failing test**

Create tests that require: source hashes remain strict; no hard-coded cross-OS compiled-manifest equality gate remains; Cholesky reconstruction residual is bounded; direct covariance quadratic form and Cholesky quadratic form agree; perturbing the factor beyond tolerance fails.

- [ ] **Step 2: Run test to verify it fails**

Run `python -m pytest -q`. Expected: the new Pantheon cross-platform contract test fails because the hard-coded `01f86af...` rejection is still present and the numerical verifier is absent.

- [ ] **Step 3: Write minimal implementation**

Add a fail-closed numerical verifier. Preserve exact Git-blob/source hashes. Store the generated manifest hash as diagnostic/platform-local provenance only. Make the compilation fail if reconstructed covariance or quadratic-form equivalence exceeds `1e-12` absolute tolerance.

- [ ] **Step 4: Update R15 semantics**

Require two independent same-platform rebuilds to have matching manifest hashes and verified scientific-equivalence receipts. Do not compare Windows generated bytes to a Linux-generated historical hash.

- [ ] **Step 5: Run tests to verify GREEN**

Run `python -m pytest -q` and `python scripts/verify_artifact.py`. Both must pass.

### Task 2: Execute adversarial R01-R16 battery

**Files:**
- Read/produce: `results/r1-robustness/R01.json` ... `R16.json`
- Produce: `results/r1-robustness/robustness-summary.json`

- [ ] **Step 1: Push the minimal fix and trigger Windows workflows**

Use the exact historical numerical runtime already frozen in `.github/workflows/r1-robustness.yml`.

- [ ] **Step 2: Inspect receipts, not only workflow status**

Require all R01-R16 receipts to exist and report `status=pass`; require robustness summary `status=verified`.

- [ ] **Step 3: If any gate fails, classify and repair root cause**

Do not lower tolerance or edit receipt status. Add a regression test before each infrastructure/code fix, then rerun.

### Task 3: Publication promotion

**Files:**
- Modify only if supported by receipts: `validation/r1_promotion.json`
- Verify: `scripts/run_validation.py`
- Regenerate: paper-facing tables/figures only from verified receipts.

- [ ] **Step 1: Run all required G01-G19 acceptance checks represented by the repository promotion contract**

Use machine-readable receipts. No manual promotion.

- [ ] **Step 2: Run strict validation**

Run `python scripts/run_validation.py --strict`. Expected: zero missing/failed required gates.

- [ ] **Step 3: Set publication-ready state only through verified promotion logic**

`PUBLICATION_READY=true` is valid only when the strict validator derives it from all required verified receipts.

- [ ] **Step 4: Freeze immutable release only after strict green**

Create the final revision tag/release from the exact verified commit; do not cite mutable `main` in the manuscript.
